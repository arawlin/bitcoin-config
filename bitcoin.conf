# Generated by https://jlopp.github.io/bitcoin-core-config-generator/

# This config should be placed in following path:
# ~/.bitcoin/bitcoin.conf

# [core]
# Run in the background as a daemon and accept commands.
daemon=0

# Maintain a full transaction index, used by the getrawtransaction rpc call.
#txindex=1

# Maximum database write batch size in bytes.
# 536870912 = 512m
dbbatchsize=536870912
# Set database cache size in megabytes; machines sync faster with a larger cache. Recommend setting as high as possible based upon machine's available RAM.
dbcache=2048

# [debug]
# Enable debug logging for all categorie
debug=1
# Log IP Addresses in debug output.
logips=1
# Log timestamps with microsecond precision.
logtimemicros=1
# Run this node on its own independent test network.
regtest=1

# [rpc]
# Accept command line and JSON-RPC commands.
server=1
# Accept public REST requests.
rest=1
# Allow JSON-RPC connections from specified source. Valid for <ip> are a single IP (e.g. 1.2.3.4), a network/netmask (e.g. 1.2.3.4/255.255.255.0) or a network/CIDR (e.g. 1.2.3.4/24). This option can be specified multiple times.
rpcallowip=127.0.0.1
rpcallowip=192.168.1.102

rpcuser=test
rpcpassword=test

# Enable Add Witness Address RPC,Enable Accounts RPC,Enable Sign Raw Transaction RPC,Enable Validate Address RPC
deprecatedrpc=addwitnessaddress
deprecatedrpc=accounts
deprecatedrpc=signrawtransaction
deprecatedrpc=validateaddress


# [Sections]
# Most options automatically apply to mainnet, testnet, and regtest networks.
# If you want to confine an option to just one network, you should add it in the relevant section.
# EXCEPTIONS: The options addnode, connect, port, bind, rpcport, rpcbind and wallet
# only apply to mainnet unless they appear in the appropriate section below.

# Options only for mainnet
[main]
# Listen for JSON-RPC connections on this port
rpcbind=127.0.0.1
rpcbind=192.168.1.105
rpcport=7001

# Options only for testnet
[test]

# Options only for regtest
[regtest]
# Listen for JSON-RPC connections on this port
rpcbind=127.0.0.1
rpcbind=192.168.1.105
rpcport=7001

